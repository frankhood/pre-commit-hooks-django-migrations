============
Installation
============

At the command line::

    $ easy_install pre-commit-hooks-django-migrations

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv pre-commit-hooks-django-migrations
    $ pip install pre-commit-hooks-django-migrations
